{
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/:path+", "destination": "/" }
  ],

  "functions": {
    "pages/api/**/*.ts": {
      "maxDuration": 200
    }
  }
}
